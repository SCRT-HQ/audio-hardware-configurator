// src/constants/grid.ts

export const GRID_SIZE = 20; // Size of each grid cell in pixels
export const DOT_SIZE = 2; // Size of each dot in pixels
export const SNAP_THRESHOLD = GRID_SIZE / 2; // Threshold for snapping to grid
